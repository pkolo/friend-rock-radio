<div class="content-container">
  <h1>Editing <%= @show.name %></h1>
  <h3>Episodes</h3>
  <% @episodes.each do |episode| %>
    <div>
      <%= link_to episode.title, edit_admin_show_episode_path(@show, episode) %>, <%= episode.created_at %>
    </div>
  <% end %>
</div>
